import { loadModels } from 'uncharted-describe-models/utils/load-models';
import { module, test } from 'qunit';

module('loadModels');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = loadModels();
  assert.ok(result);
});
